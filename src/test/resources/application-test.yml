hotelbooking:
  database:
    url: jdbc:h2:mem:kpi;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: sa

spring:
  datasource:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
