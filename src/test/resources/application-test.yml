hotelbooking:
  database:
    url: jdbc:h2:mem:kpi;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: sa
  tomcat:
    workdir: build/tomcat-workdir

spring:
  datasource:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
